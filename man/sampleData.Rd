% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/methods-sampleData.R
\docType{methods}
\name{sampleData}
\alias{sampleData}
\alias{sampleData<-}
\alias{sampleData}
\alias{sampleData,SummarizedExperiment-method}
\alias{sampleData<-,SummarizedExperiment,DataFrame-method}
\title{Sample Data}
\usage{
sampleData(object, ...)

sampleData(object, ...) <- value

\S4method{sampleData}{SummarizedExperiment}(object, interestingGroups,
  isFactor = TRUE, blacklist = TRUE, return = c("DataFrame", "data.frame",
  "kable"))

\S4method{sampleData}{SummarizedExperiment,DataFrame}(object) <- value
}
\arguments{
\item{object}{Object.}

\item{...}{Additional arguments.}

\item{value}{Value to assign.}

\item{interestingGroups}{\code{character} vector defining the groups of interest.}

\item{isFactor}{Only include \code{factor} columns. Generally recommended. This
helps easily discard columns in \code{\link[=colData]{colData()}} that are \code{numeric} metrics.}

\item{blacklist}{Drop blacklisted columns defined in \link{metadataBlacklist}.
Only applies when \code{isFactor = TRUE}.}

\item{return}{Object class to return. Uses \code{\link[=match.arg]{match.arg()}} internally and picks
the first item in the vector by default.}
}
\value{
Data describing the samples.
}
\description{
Return the sample metadata. Columns are always sanitized to factor.
}
\note{
This is a complement to the standard \code{\link[=colData]{colData()}} function, but improves
support for accessing sample metadata for datasets where multiple items in
the columns map to a single sample (e.g. cells for a single-cell RNA-seq
experiment).
}
\examples{
# SummarizedExperiment ====
sampleData(rse_dds)

# Assignment support
x <- rse_dds
sampleData(x)[["test"]] <- seq_len(ncol(x))
# `test` column should be now defined
glimpse(sampleData(x))
}
\seealso{
\link{metadataBlacklist}.
}
\author{
Michael Steinbaugh
}
